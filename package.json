{
  "name": "conflab",
  "version": "0.0.3",
  "description": "A configuration library that probably does more than it should.",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/tes/conflab.git"
  },
  "scripts": {
    "test": "NODE_ENV=test istanbul cover _mocha -- -R spec tests/* && istanbul check-coverage --statements 80",
    "lint": "jshint ."
  },
  "precommit": [
    "lint",
    "test"
  ],
  "license": "MIT",
  "dependencies": {
    "async": "^0.9.0",
    "lodash": "^2.4.1",
    "minimist": "^1.1.0",
    "node-etcd": "^3.0.0",
    "pathval": "^0.1.1"
  },
  "devDependencies": {
    "expect.js": "^0.3.1",
    "istanbul": "^0.3.2",
    "jshint": "^2.5.7",
    "mocha": "^2.0.1",
    "precommit-hook": "^1.0.7"
  }
}
